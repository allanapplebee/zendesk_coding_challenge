<% include ./partials/header %>
    <body>
        <div class="container">
            <% include ./partials/jumbotron %>
            <% tickets.tickets.map(ticket => { %>
                <p><%= ticket.subject %>
                    created by 
                    <%= ticket.requester_id  %>
                    on 
                    <%= ticket.created_at.slice(0, 10) %>
                    with ID
                    <%= ticket.id %>
                </p>
            <% }) %> 
            
            <div class="row">
                <div class="col">
                    <% if(tickets.previous_page !== null) { %>
                        <form action="/listall" method="POST">
                            <input type="hidden" name="url" value=<%= tickets.previous_page %>>
                            <button type="submit" class="btn btn-primary">Previous Page</button>
                        </form> 
                    <% } %>
                </div>
                <div class="col">
                    <% if(tickets.next_page !== null) { %>
                        <form action="/listall" method="POST">
                            <input type="hidden" name="url" value=<%= tickets.next_page %>>
                            <button type="submit" class="btn btn-primary pull-right">Next Page</button>
                        </form>
                    <% } %>  
                </div>
            </div>
            <div class="row">
                <div class="col">
                <a href="/" class="btn btn-primary">Home</a>
                </div>
            </div>
        </div>
    </body>
</html>